{
  "name": "lighthouse-bsd",
  "version": "1.0.0",
  "private": true,
  "description": "Lighthouse International Learning Center — BSD City branch website",
  "scripts": {
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4"
  },
  "devDependencies": {
    "vercel": "^56.2.1"
  },
  "engines": {
    "node": ">=20"
  }
}
